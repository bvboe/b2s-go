apiVersion: v1
kind: Service
metadata:
  name: {{ include "bjorn2scan.fullname" . }}
  labels:
    {{- include "bjorn2scan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "bjorn2scan.selectorLabels" . | nindent 4 }}
